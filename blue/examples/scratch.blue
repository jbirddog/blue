extern exit ( status:edi -- noret )

global _start

section .rodata
section .data
section .bss
section .text

: #!@$% ( status:edi -- noret ) exit ;

\
\ Entry point into the program
\ Only explaining this to test comments
\
: _start ( -- noret ) 0 #!@$% ;
