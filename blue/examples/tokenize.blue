import sys

global _start

: syscall3 ( edi edx esi num:eax -- result:eax ) syscall ;
: unwrap ( result:eax -- value:eax ) dup 0 cmp ' exit.syserr xl ;

: read ( fd len buf -- result ) 0 syscall3 ;

1024 const buf.cap 

buf.cap resb buf

: read ( fd -- read ) buf.cap buf read unwrap ;

hide buf

: _start ( -- noret ) stdin read exit.ok ;
