global _start

: syscall ( num:eax -- result:eax ) syscall ;

: exit ( status:edi -- noret ) 60 syscall ;
: bye ( -- noret ) 0 exit ;

1 const stdout

: write ( buf:esi len:edx fd:edi -- ) 1 syscall drop ;
: write ( buf len -- ) stdout write ;

: greet ( -- ) s" Hello world!\n" write ;

: _start ( -- noret ) greet bye ;
